"""
Builds the system + user prompt from the versioned template, retrieved
context (tables, join paths, business terms), and the user's question.

Not yet implemented — see enterprise-text-to-sql-architecture.md §3.
"""
