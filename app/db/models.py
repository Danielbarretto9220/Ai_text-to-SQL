"""
ORM/typed models for the meta.* schema (meta.tables, meta.columns,
meta.relationships, meta.business_glossary, meta.query_patterns,
meta.document_embeddings). The schema itself currently lives as raw SQL
in METADATA/01-10*.sql; this module would give it a typed Python layer.

Not yet implemented — see enterprise-text-to-sql-architecture.md §1.2.
"""
