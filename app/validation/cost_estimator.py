"""
Runs EXPLAIN (FORMAT JSON) on validated SQL before execution and rejects
queries whose estimated cost/rows exceed a configurable threshold.

Not yet implemented — see enterprise-text-to-sql-architecture.md §5.
"""
