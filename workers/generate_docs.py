"""
Introspects Postgres (information_schema, pg_constraint, pg_description)
to auto-generate the base Markdown doc skeleton per table, which SMEs
then enrich with business descriptions.

Not yet implemented — see enterprise-text-to-sql-architecture.md §1.7.
"""
